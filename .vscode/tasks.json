{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "gcc build active file",
            "command": "bash",
            "args": [
                "-c",
                "gcc -g \"${workspaceFolder}/Game Code\"/*.c -o \"${workspaceFolder}/game\""
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compiles the entire project"
        }
    ]
}
