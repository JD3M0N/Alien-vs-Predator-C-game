{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/game",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "gcc build active file",
        }
    ]
}
